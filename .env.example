# EVT Umgebungsvariablen für Raspberry Pi
# Kopiere diese Datei zu .env und fülle die Werte aus

# Database Connection
DATABASE_URL=postgresql://evt_user:DEIN_PASSWORT@localhost:5432/evt_db

# PostgreSQL einzelne Werte (optional)
PGHOST=localhost
PGPORT=5432
PGUSER=evt_user
PGPASSWORD=DEIN_PASSWORT
PGDATABASE=evt_db

# Session Secret (ändere zu einem langen zufälligen String!)
SESSION_SECRET=GeneriereEinenLangenZufaelligenString123!

# Push Notifications (VAPID Keys)
# Generiere mit: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=dein_vapid_public_key_hier
VAPID_PRIVATE_KEY=dein_vapid_private_key_hier
VAPID_SUBJECT=mailto:admin@evt-ofwschwedt.de

# Application
NODE_ENV=production
PORT=5000
